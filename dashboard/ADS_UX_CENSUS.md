# ADS UX — INTERACTION CENSUS (2026-08-07, pre/post the #127 build)

Every rendered element on /ads, across tabs (Ads/Names/Batches/Campaigns/Account):
clickable? → what opens? If static → deliberate (reason) or gap (→ built).

| # | element | before | after (#127) |
|---|---|---|---|
| 1 | **Sets "0 ⚠/◔N" badge (B008_A04 — the witnessed dead-end)** | tooltip only, DEAD | **DOOR** → anomaly panel: plain-English cause, the deal(s), tracker evidence (Set Date BLANK), GHL link, queue state, resolution lane |
| 2 | ↤N earlier-closes / earlier-sets / earlier-shows badges | tooltip only | DOOR → same anomaly panel, scoped per type |
| 3 | metric cells (leads/qualified/reached/sets/shows/closes) | drill (roster) ✓ | unchanged ✓ + roster inherits clock AND market |
| 4 | creative NAME cell | row-select filter | **DOOR → the dossier** (identity · unit econ window+all-time · lead ledger w/ chips+provenance+links); row-select still on the rest of the row |
| 5 | verdict pill | tooltip (driver) | deliberate-static: the driver text IS the evidence; full context lives in the dossier — reason logged |
| 6 | provisional badge | tooltip (why+progress) | deliberate-static: same reason as #5 |
| 7 | headline tiles | static | static + **Δ vs prior equal window** line (engine-computed, labelled); deliberate-static beyond that (totals ARE the aggregate — their doors are the grid) |
| 8 | activity cash strip | static label | deliberate-static: it names its clock; the closes drill is the door |
| 9 | hygiene disagreement items | text only | DOOR → deal panel (name extracted); **Dateless rail** = first-class bucket, each item a door, self-clearing on sync |
| 10 | identity strip | static | deliberate-static: rates are account-level; detail = JOIN_CONTRACT.md — logged |
| 11 | flags cards (scorecard) | static text | deliberate-static THIS build: flags carry creative names whose dossiers are one click away on the grid; full flag→dossier link logged as PROPOSED (small) |
| 12 | tier rows (Unattributed/Ambiguous/IG DM) | drill via cells ✓ | unchanged ✓ + stay PINNED below ad rows under every sort |
| 13 | window control | 30/60/90 | +**All** · URL ?window= (incl. all) · maturity labels per active clock (#120 toggle) |
| 14 | basis toggle | ✓ (#120) | unchanged; drill headers state the clock |
| 15 | market | ABSENT | **All/AU/US/Unknown chips** — engine-level filter (tracker Market col, 100% coverage), I15 partition, URL ?market= |
| 16 | sort | header click ✓ (no URL, no presets) | + presets menu (incl. Lowest performing = verdict ranking) · URL ?sort= · stable secondary spend-desc · tiers pinned |
| 17 | find | lead-rows search ✓ | + grid find over creative/campaign/batch (presentation-only, "FILTERED VIEW" note) |
| 18 | wide table | plain scroll | sticky header + frozen Creative column + column visibility picker (persisted) |
| 19 | lead rows table | row → person drill ✓ | unchanged ✓ |
| 20 | ACTION FEED (finance dashboard) object items | text only (El Gringos witnessed) | **deep-link** → /ads?deal=<name> opens the deal panel (blank-date items carry deal_name) |

Gaps → built: #1, #2, #4, #9, #13, #15, #16(+), #17(+), #18, #20.
Deliberate-statics with reasons: #5, #6, #7(partial), #8, #10, #11 (PROPOSED: flag→dossier link).

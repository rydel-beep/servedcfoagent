# RANGE-AWARE UNIT ECONOMICS — Phase 0 (definitions + date map) — HARD STOP

**Date:** 2026-06-25 (Sydney) · **Scope:** served-cfo-agent only · **Status:** HARD STOP — confirm
the ROAS revenue basis, the LTV definition, and the attribution basis before building. Nothing built.

Goal: compute LTGP:CAC, ROAS, LTV:CAC for ANY date range (voice/text) with **window-consistency by
construction** — one `range_start/range_end` drives every input, so mixing windows is impossible.

## 1. Locked definitions (from prior rounds — DECISIONS 56–58)

| Metric | Formula | Basis |
|---|---|---|
| **CAC (loaded)** | (Meta ad spend + closer comms + setter comms) / closes | all in-window; ad spend Meta-only (Google hook noted) |
| **LTGP** | avg **contract value** × gross margin % | Rydel-confirmed "Lifetime" = contract value (not cash collected). Collection-adjusted variant = future option, labelled |
| **LTGP:CAC** | LTGP / loaded CAC | — |
| **ROAS** | revenue / **ad spend only** | AD-SPEND-ONLY (no comms), distinct from CAC. *Revenue basis = TBC (Q below)* |
| **LTV:CAC** | LTV / loaded CAC | *LTV def = TBC (Q below)* |

Today's code: ROAS (m8) = (closes × avg_contract) / ad_spend = **contracted** revenue ÷ spend.
LTV:CAC (m7) = avg_contract / CAC = **full contract value, no margin** ÷ CAC. (So LTV today =
contract value *without* margin; LTGP = contract value *with* margin — they legitimately differ.)
Gross margin % is a P&L rate (Xero, ~71%), applied to the window's contract value — it's a rate,
not a windowed sum.

## 2. Date-column map (which date windows each input) — mirror-backed

| Input | Mirror tab | Column(s) | Windowed by |
|---|---|---|---|
| Closes (count) + the deals | Lead-to-Cash Tracker | **Close Date (col 27)** | Close Date |
| Contract value (per deal) | Lead-to-Cash Tracker | Contract Value (col 28) | the deal's Close Date |
| Cash collected (per deal) | Lead-to-Cash Tracker | Cash Collected (col 32) | the deal's Close Date |
| Closer comms (per deal) | Lead-to-Cash Tracker | Commission Closer (col 40) | the deal's Close Date |
| Setter comms ($50 + 5%) | SETTER PAYOUT LOG | set_fee + pct_bonus | **payout date** (the log has no set/close-date column; tracker name-join matched only 7/164 — payout date is the reliable key, as established in the loaded-CAC round) |
| Meta ad spend | (Meta daily store, not a sheet) | per-day spend | spend date |

The Lead-to-Cash Tracker is **per-deal and dated** (1,198 data rows, 53 cols) → closes / contract /
cash / closer-comms all window cleanly off **Close Date**. The one wrinkle: setter comms window off
**payout date** (no close-date in the log) — same labelled caveat as the loaded-CAC build.

## 3. Attribution basis (the subtle trap — your call)

For a range, "ad spend" can mean:
- **(a) spend that OCCURRED in the window** — May's spend = what Meta charged in May. Simplest,
  most defensible, matches how spend is reported. **Recommended.**
- **(b) spend ATTRIBUTED to deals that CLOSED in the window** — May's closes were generated by
  earlier spend (lead→close lag), so you'd shift spend by the sales cycle. More "true CAC" in
  theory, but needs a lag assumption and is fragile.

I recommend **(a) spend-in-window** as the default (and apply it consistently), with (b) as a future
option. Whatever you pick, every metric uses it the same way.

## 4. Granularity readiness
- Lead-to-Cash Tracker: per-deal rows with Close Date + contract + cash + closer comm → **ready**.
- SETTER PAYOUT LOG: per-deal $50/5%, payout-dated → **ready** (payout-date window, labelled).
- Meta spend: per-day store → **ready** for any window.
- Gross margin: Xero P&L rate (not per-window) → applied as a rate. **OK.**
All read from the **mirror** (DB-speed). A custom range = a DB filter + arithmetic, near-instant.

## HARD STOP — confirm before building
1. **ROAS revenue basis:** **contracted** (closes × avg contract value, current) or **cash
   collected** (sum of in-window cash)? (Affects the ROAS number; stays ad-spend-only either way.)
2. **LTV (for LTV:CAC):** keep **full contract value, no margin** (current) — or define LTV
   differently (e.g. contract value × margin = same as LTGP, or include expected renewals)?
3. **Attribution basis:** **(a) spend-in-window** (recommended) or **(b) attributed-to-closes**?

CAC (loaded), LTGP (contract × margin), and LTGP:CAC are already locked from prior rounds — no
change unless you say so.

---

## Phases 1–4 — built (confirmed: spend-in-window · cash-ROAS · contract-value LTV)

**`range_unit_economics.py` (new):** `unit_economics(range_start, range_end)` — a single window
drives EVERY input (window-consistent by construction). From the **mirror**:
- closes / contract value / cash / closer comms ← Lead-to-Cash Tracker, windowed by **Close Date**
  (columns resolved by header name, robust to reordering).
- setter comms ($50 + 5%) ← SETTER PAYOUT LOG, windowed by **payout date**.
- ad spend ← `meta_spend.spend_in_range` (daily store when it covers the range, else one live
  Insights call) — **spend-in-window** attribution.
- gross margin ← Xero P&L rate, applied to the window's contract value.
Computes: CAC (loaded), LTGP:CAC, **ROAS = cash collected ÷ ad spend**, **LTV:CAC = avg contract ÷
CAC**. Returns every ratio WITH its full component breakdown (spend/closer/setter/closes/contract/
cash) + caveats. **No div-by-zero**: zero closes → ratios null + "undefined" message; spend-but-no-
closes flagged; small/short windows flagged as volatile.

**NL range parsing (Phase 2):** "in May", "last 3 weeks", "between X and Y", "this/last month",
"Q1 2026", "YTD", "today/yesterday", "last 30 days" (anchored on today_sydney). No range → defaults
to last 30 days and says so. Wired into the chat router (voice+text), AFTER the targets handler so
"set the LTGP:CAC target" still routes to targets.

**Driver decomposition (Phase 3):** every answer carries the breakdown ("LTGP:CAC for May: 2.x× —
LTGP $Y ÷ CAC $Z (ad $a + closer $b + setter $c ÷ N closes)"). **Comparisons** ("this month vs last
month") compute both windows and attribute the change to its drivers (spend Δ, closes Δ, contract Δ,
cash Δ).

**Dashboard consistency (Phase 4):** `GET /dashboard/api/unit-economics?start=&end=` (or `?days=N`)
routes through the SAME engine, so the dashboard window buttons and EDITH's spoken answers can't
drift for the same window. Labels intact: CAC loaded (Meta-only), ROAS ad-spend-only (cash basis),
LTV:CAC (contract value), LTGP (contract × margin); "as of" from the mirror.

**Tests:** 252 pass (+6: range parsing, window-consistent May math, zero/spend-no-close edges,
cash-ROAS basis, command handler). Acceptance gate (voice "LTGP:CAC in May" → correct
window-consistent figure + driver explanation) verified on the deployed app.

---

## Closes reconciliation + dashboard wired through the engine (follow-up)

**The discrepancy (diagnosed):** the range engine first counted a "close" as any row with a Close
Date + contract value (loose) — 8 in trailing-30d. The dashboard's CAC tile used the **Team
Scorecard's pre-computed `closes` cell** (4, opaque formula, fixed window). Two methodologies → two
CACs ($2.2k vs $4.4k) for the same window.

**The fix (one canonical methodology):** a close = **Call Outcome (col 23) == "won"** with a Close
Date in the window — the same definition the velocity calc already uses. Transparent, per-deal, and
range-flexible (the Scorecard cell can't be re-windowed). The loose `contract>0 OR offer` test is
gone. `_ltc_col_map` now resolves the Call Outcome column by header; `_ltc_in_window` requires
`outcome == "won"`. (The Scorecard's `closes` cell remains for the legacy funnel display, but it is
no longer what drives unit economics.)

**Dashboard wired through the engine (Phase 4 complete):** the CAC / LTGP:CAC / LTV:CAC / ROAS tiles
now call `GET /dashboard/api/unit-economics?days=<selected window>` (`applyRangeEconomics`) on load
and on every window-button / perf-tab change — so the **tiles and EDITH's spoken answers use one
engine and can't drift**. Each tile carries the full breakdown on hover (window + components +
basis). Only `dashboard.js` changed (the parallel session's `dashboard.html` untouched). Tests: 252
pass; engine close-count test updated to assert Call-Outcome-won (a "lost" deal in-window is
excluded).
